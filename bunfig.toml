[install]
# Don't install any package version published less than 7 days ago. Recent npm supply-chain
# attacks have pushed malicious releases that get yanked within hours/days, so the cooldown lets
# the wider ecosystem catch them before they reach us. Value is in seconds (7 * 24 * 60 * 60).
minimumReleaseAge = 604800
